import { config } from '../config/config';
import { DataStore } from '../datastore/datastore';
import { Logger } from '../output/logger';

// Use handler classes to schedule long, ongoing, sequential or periodic tasks, such as creating and hosting a site.

export class SiteHandler
{
  public static createSite()
  {

  }
}
